// Coloque aqui suas actions
export const EMAIL_CHANGE = (email) => ({
  type: 'EMAIL_CHANGE',
  email,
});

export const PASSWORD_CHANGE = (password) => ({
  type: 'PASSWORD_CHANGE',
  password,
});
